import Canvas from './Canvas';

class FireVector extends Canvas {
    /**
     * Class that represents a line as drag-n-drop vector to define angle and Vo of element fly
     * @constructor
     */
    constructor() {
        super();
        this.background = 'brown';
    }
}

export default FireVector;
